# 开发进度

> 接手时先读本文件。每完成一步都要更新这里，然后 git commit。
> 最后更新：2026-09-24 · 对话点评代码兜底（This is + 名字、微信号 reach me at）

## 总体阶段

| 阶段 | 内容 | 状态 |
|---|---|---|
| 1 | 项目骨架、本地服务接口、启动脚本、页面框架和导航 | ✅ 完成 |
| 2 | 表达卡和间隔复习（PRD 3.3） | ✅ 完成 |
| 3 | AI 情景对话和复盘（PRD 3.2） | ✅ 完成 |
| 4 | 跟读、独白：语音识别与评分（PRD 3.4、3.5、5.2） | ✅ 完成 |
| 5 | 学习计划、每日随机课程、笔记本、进度、Notion 同步（PRD 2.3、3.1、3.6–3.9） | ✅ 完成 |
| 6 | 入门测评、一键卸载 uninstall.sh、整体联调（PRD 2.4、3.11、第 8 节验收） | ✅ 完成 |

## 阶段 1 已完成

- 后端：Express + sql.js；`schema.sql` 已建好 PRD 6.1 全部表，另加 `settings` 表。
- 接口：`GET /api/health`（对话模型 / 语音识别 / Notion 状态及修复提示），`GET|PUT /api/settings`（Token 不回传明文），`POST /api/llm/chat`（转发 Ollama）。
- 服务封装：`llm.js` 已可用；`asr.js` 只做了状态检测，`transcribe()` 待阶段 4；`score.js`、`notion.sync()` 为占位。
- 前端：React + HashRouter，侧栏导航与原型一致；「今日」为框架；「设置」页已可用（服务状态、模型名、朗读声音试听、语速、每日时长、提醒时间、Notion Token）；其他页面为占位页，标明所属阶段。
- 朗读：`web/src/services/tts.js` 已可用。
- 启动脚本 `scripts/start.sh`：检查 Node、安装依赖、检查 Ollama 与模型（可一键拉取）、检查 whisper.cpp 与模型（可一键下载）、构建、启动并打开浏览器。
- 测试：`tests/api.test.js` 4 项通过。
- 用户反馈修改：服务连不上时设置页提示重新运行启动脚本；朗读声音保留在线声音（如 Google），无法使用时在试听处提示。

## 阶段 2 已完成

- 内容：`content/week01～03.json`，每周 30 张表达卡（中文、英文、替换例句）。启动时由 `server/content.js` 导入 `content_item` 并生成 `card` 行，可重复运行；修改 JSON 后只会同步到还没学过的卡。
- 算法：`server/services/srs.js`（纯函数）。没想起 → 1 天且本轮末尾再出一次；想起但卡 → 间隔 ×1.2 向上取整；脱口而出 → 间隔 ×2.5 四舍五入；起点 1 天；连续 3 次脱口而出且间隔 ≥ 14 天 = 熟练。实际序列：脱口而出 3 → 8 → 20 天。
- 每日队列：`server/cards.js` 的 `todayQueue()`，先到期卡，再补新卡（只取当前周及以前）。上限可在设置页调：每日新卡（`cardsNewPerDay`，默认 30）、每日总张数（`cardsDailyMax`，0 = 不限，默认不限）。第几天/第几周由设置里的 `startDate` 计算，首次使用时自动设为当天（阶段 6 入门测评完成时可重设）。
- 接口：`GET /api/cards/today`、`POST /api/cards/:id/review {rating:1|2|3}`、`POST /api/cards`（手动或从对话/独白加入，阶段 3、4 复用 `addCard()`）。
- 数据库：`card` 新增 `content_id, introduced_at, reviewed_at, last_rating`，旧库启动时由 `db.js` 的 `migrate()` 自动补字段。
- 页面：`web/src/pages/Cards.jsx`，翻面、朗读（英文；「也可以」一行后有小喇叭）、三档自评并显示预计间隔、进度条、完成页、快捷键（空格翻面，1/2/3 自评）。
- 测试：共 11 项通过（新增 `srs.test.js`、`cards.test.js`）。

### 阶段 2 暂未做（已记入后续阶段）

- 内容量：默认每日新卡 30，现有第 1–3 周共 90 张，用户决定以后再补（每周扩到 60–100 张 / 提前写第 4–12 周 / 加自选主题卡组）。添加方法见 README「如何添加学习内容」。
- 自选卡组（不按周解锁、单独每日额度、可暂停）：未做，待用户确认后排期。

- 卡片正面「先录音再对照」：需要语音识别，阶段 4 加。
- 内置内容审核开关（PRD 第 7 节）：JSON 里有 `reviewed` 字段，但目前不拦截未审核内容；阶段 5 在设置页加审核入口。
- 复习记录写入 `session` 表：阶段 5 做每日课程时统一处理。

## 阶段 3 已完成

- 内容：第 1–3 周每周 2 个场景（基础版 / 进阶版），写在 `content/weekNN.json` 的 `scenes` 数组：AI 角色设定（英文 persona）、开场白、中文场景说明、3–4 项任务（`zh` 给用户看，`check` 给模型判断用）、提示句。导入到 `content_scene.data`。
- 后端：`server/roleplay.js`。每轮一次模型调用，要求 JSON 输出 `{reply, reply_zh, coach, completed}`：回复 ≤ 20 词、一次只问一个问题、不纠错；用户打中文时在 `coach` 给英文说法。任务完成情况取并集累计。结束时再调用一次生成复盘 `{comment_zh, fixes[]}`。通关 = 任务全部完成且用户发言 ≥ 8 轮。
- `llm.chatJSON()`：JSON 解析失败时兜底为纯文本回复。Ollama 不可用返回 503 和中文提示。
- 接口：`GET /api/roleplay/scenes`、`POST /api/roleplay {sceneId}`、`GET /api/roleplay/:id`、`POST /api/roleplay/:id/turn {text}`、`POST /api/roleplay/:id/finish`。
- 页面：`web/src/pages/Roleplay.jsx`。场景列表（按周解锁、状态）、对话页（AI 回复自动朗读、再听 / 慢速 / 中文、语速 0.8x/1.0x、右侧任务 / 提示 / 场景面板）、复盘弹窗（地道说法可朗读、一键加入表达卡、再练一次）。
- 测试：`tests/roleplay.test.js` 用假模型（环境变量 `SPEAK90_FAKE_LLM=1`）跑通流程，共 17 项通过。
- README 增加「如何添加学习内容」；导入规则放宽为 `week` 开头的任意 json（如 `week03-work.json`）。

- 用户实测：qwen2.5:7b 回复 3–5 秒，可接受。
- 模型调用：`llm.chat()` 传 `think: false`（关闭 Gemma 4 / Qwen3 等的思考模式，用户实测 gemma4:12b 未关闭时一轮约 1 分钟），模型不支持该参数返回 400 时自动去掉重试；`keep_alive: 30m` 让模型在练习期间常驻内存。
- 修正（用户反馈 bug：已说了工作但任务没勾上）：任务判定改为单独调用 `checkTasks()`（只判断未完成任务、温度 0、按意思宽松判断），前端在 AI 回复后调用 `POST /api/roleplay/:id/tasks`，再做点评；结束复盘前再检查一次。回复里模型给的 completed 仍取并集。
- 修正：点评如果只是大小写、标点不同，判为「表达自然」。
- 修正：AI 只在用户打中文或问「怎么说」时给「可以说…」提示（提示词收紧 + `roleplay.js` 服务端兜底）。
- 修正（用户反馈 bug）：用户打中文时，先单独调用 `translateLine()` 把这句中文翻成语境下的口语英文，作为「可以说」和该句点评；不再依赖对话模型的 coach 字段（它曾把 AI 自己的问题当成提示），也避免点评把中文判为「表达自然」。中文输入的回复会多等 2–3 秒。

### 对话每句点评（用户要求新增，PRD 3.2 补充）

- 用户每发一句，AI 回复后前端再调 `POST /api/roleplay/:id/feedback {index}`（放在回复之后，避免 Ollama 排队拖慢回复）。`roleplay.feedback()` 让模型判断这句是否自然，输出 `{ok}` 或 `{ok:false, better, issue_zh, zh}`，存在 `roleplay.feedback` 列（按消息序号）。
- 页面：点评直接展开显示在用户气泡下方（「✓ 表达自然」或「可以更地道」+ 更好说法 + 中文说明 + 朗读 + 加入表达卡）。
- 复盘改为汇总全部问题句（先补齐未点评的句子），模型只再生成一句中文总评。
- 纠错准确度受当前 7B 模型限制；用户已决定「先跑通流程，模型后面再换」，可考虑只给点评这一步单独配大一点的模型（如 14B）。

### 阶段 4 已提前完成：对话页语音输入（用户要求优先）

- 前端 `web/src/services/recorder.js`：麦克风 → AudioContext 16kHz → 16-bit WAV（不依赖 ffmpeg），最长 60 秒；录音时停止朗读。
- 后端 `POST /api/asr`（body 为 audio/wav）：存到 `data/audio/`，调用 `asr.transcribe()`（whisper-cli `-ojf`），返回文字和词级 `{word,start,end,prob}`（毫秒），写入 `recording` 表。子词合并逻辑 `tokensToWords()`。
- 对话页：麦克风按钮点一下开始、再点一下结束 → 识别文字填入输入框可修改 → 发送；该轮消息记录 `recordingId`，供之后做流利度分析。
- 注意：`small.en` 只识别英文，说中文识别不了，所以求助仍需打字。
- 测试：新增 `tests/asr.test.js`（假识别 `SPEAK90_FAKE_ASR=1`），共 21 项通过（加每句点评后 22 项）。真实 whisper 需在用户 Mac 上验证。

### 阶段 3 暂未做

- 复盘里的流利度（语速、停顿）：录音已存，阶段 4 做 `score.fluency()` 后接入。

## 阶段 4 已完成

- 内容：第 1–3 周每周 2 组跟读材料（5–6 句，`shadow` 数组）、5 个独白话题（`topics` 数组），导入 `content_item`（type = shadow / topic）。PRD 要求每周 10 段跟读，目前每周 2 段，内容待补。
- 评分 `server/services/score.js`（纯函数）：`align()` 原文与识别结果逐词编辑距离对齐 → 读对 / 不确定（置信度 < 0.5）/ 漏读 / 读错，完整度 %；常见缩写展开（I'm = I am）。`fluency()` 语速（去掉口头禅）、> 2 秒停顿次数、最长停顿、口头禅（um/uh/er…）。`mergeFluency()` 合并多段。
- 后端 `server/practice.js`：`GET /api/shadow`、`POST /api/shadow/score {recordingId, reference}`、`GET /api/mono/topics`（已解锁周）、`POST /api/mono {topicId, recordingId}`（流利度 + 模型改写 `{rewrite, phrases, comment_zh}`，记入 `session` 表 module = mono）。
- 对话复盘增加语音部分流利度（只统计用语音说的句子）。
- 页面：`Shadow.jsx`（材料选择、逐句 / 影子、0.75/1.0/1.2x、隐藏原文、听原句、录音、回放、逐词标色、漏读/读错说明）；`Mono.jsx`（随机话题、30 秒准备自动开始、60 秒自动结束、识别文字、地道版本高亮改动、值得学的表达加入表达卡）。录音 Hook `web/src/services/useMic.js`。
- 影子跟读（用户反馈后改）：逐句播放，每句播完留出跟说时间（词数 × 0.45 秒 ÷ 语速 + 1.5 秒，有倒计时，可点「说完了」提前下一句），全程录音，结束后整段评分。原先连续播放全文、用户没有开口空档。建议戴耳机。离开页面或点「停止」会中止播放、丢弃本次录音、不评分；全局切换页面时停止朗读（`App.jsx`）。
- 测试：新增 `score.test.js`、`practice.test.js`，共 30 项通过。

### 阶段 4 暂未做

- 表达卡正面「先录音再对照」：未做（用户未要求优先）。
- 跟读「单句循环」：原型有，未做。
- whisper 常把 um/uh 省略，口头禅统计可能偏少。

## 划词查询与笔记本（用户新需求，已完成）

- 全局组件 `web/src/components/SelectionLookup.jsx`：任意页面选中英文词或短语（≤ 6 词、不含中文、不在输入框内）→ 浮出「查词」→ `POST /api/lookup {text, context}`，本地模型结合原句解释 `{zh, pos, usage_zh, example, example_zh}` → 「加入笔记本」「加入表达卡」两个按钮（用户选择：只进笔记本，表达卡需手动点）。来源按当前页面记录。
- 表达卡：单击翻面，双击或拖动选词时不翻面（点击延迟 250ms 判断）。
- 笔记本（PRD 3.7 提前做的可用版）：`server/notes.js`，`GET /api/notes?q=&source=`、`POST /api/notes`（按英文去重）、`DELETE /api/notes/:id`；页面 `Notes.jsx`：搜索、按来源筛选、点英文展开释义和例句、朗读、加入表达卡、删除、手动添加。
- `note` 表增加 `context`（原句）、`detail`（释义 JSON）。
- 音标：`server/services/ipa.js` 读本地词典 `server/data/ipa_en_US.txt`（open-dict-data/ipa-dict，MIT，约 3MB），取第一种读音，ɹ→r、ɫ→l；短语逐词拼接，有词查不到则不显示。笔记本列表显示「词 /音标/ 词性」，划词浮窗显示音标。词性来自划词时模型给的 `detail.pos`（手动添加的没有）。
- 未做：收藏筛选、Notion 同步（阶段 5）；自动收集错句到笔记本（目前错句通过点评/复盘手动加入表达卡）。
- 测试：新增 `notes.test.js`，共 32 项通过。

## 阶段 5a 已完成：每日课程、学习计划、进度

- 活动记录：新表 `daily_log(date, module, count)`，`server/activity.js` 的 `logEvent()`；表达卡自评、跟读评分、独白、对话每轮/结束时记录。用于完成度、日历、连续打卡。
- 每日课程 `server/plan.js`（PRD 2.3）：首次打开当天生成 `plan_day`：热身表达卡 5 分钟 → 两个随机 10 分钟 → 收尾 5 分钟。随机只在跟读 / AI 对话 / 独白里抽（热身已是表达卡），按阶段权重（PRD 2.1），近 2 天没出现的 ×2，不放回。周日为复习日（表达卡 + 本周错句重说）。每周第 6 天：随机 A 换成本周进阶版场景「通关挑战」。换一个：每天 1 次，只换还没开始的随机项。
- 完成判定：表达卡 = 当天队列清空或复习数 ≥ 生成时的待复习数（`cards_target`）；AI 对话 = 当天有结束的对话（挑战日要求指定场景）；跟读 = 当天评分 ≥ 5 句；独白 = 当天完成 1 次；收尾 = 点「完成收尾」（`wrap_done`）。收尾弹窗列出今天对话点评里的问题句和独白里值得学的表达，可加入表达卡。
- 接口：`GET /api/plan/today`、`POST /api/plan/swap`、`GET|POST /api/plan/wrap`、`GET /api/plan`（90 天日历完成度、阶段进度、12 周路线）、`GET /api/progress`（最近 3 次独白平均语速/长停顿、场景通关、熟练卡、累计开口分钟 = 所有录音时长、连续打卡、徽章、语速趋势）。
- 页面：`Today.jsx`（任务列表点击直达对应练习：对话直接开对应场景、跟读定位到对应材料）、`Plan.jsx`、`Progress.jsx`。
- 测试：新增 `plan.test.js`，共 37 项通过。
- 暂未做：测评记录（阶段 6 入门测评）；「每日时长」设置目前不影响课程时长（固定 30 分钟）；浏览器提醒通知。

## 阶段 5b 已完成：Notion 同步

- `server/services/notion.js`，API 版本 `2022-06-28`（数据库由本工具创建，单数据源，官方表示旧版本继续可用；若以后 Notion 弃用需升级到 2025-09-03 的 data_sources 接口）。
- 设置页三步：填 Token → 粘贴已连接集成的父页面链接，点「创建数据库」自动建「口语笔记」（英文/中文/音标/词性/来源/原句/日期）和「每日练习记录」（标题/日期/练习模块/完成度/表达卡复习/对话轮数/跟读句数/独白次数/开口分钟）→ 立即同步 / 自动同步开关。
- 同步规则：单向；笔记按 `note.synced` 只推新增；每日记录按 `daily_log` 有活动的日期，内容哈希变化才更新（`notion_sync` 表）。自动同步：服务启动 30 秒后、每 10 分钟、完成每日收尾时；断网跳过，下次补。本地删除笔记不会删 Notion 里的。
- 笔记本页加「同步到 Notion」按钮和未同步数。
- 测试：`tests/notion.test.js` 用本地假 Notion 服务（环境变量 `NOTION_API_URL`），共 39 项通过。真实账号未验证。
- 未做：表达卡复习状态同步到 Notion（PRD 6.2 的熟练度 / 下次复习）；只同步了笔记本。

## 阶段 6 已完成：测评、卸载、导出、验收

- 入门测评（PRD 2.4）：`server/assessment.js` + `web/src/pages/Assessment.jsx`，路由 `/assessment`。流程：3 句跟读 → 1 分钟自我介绍 → 3 个问答（问题用系统朗读播放，**不是**实时 AI 对话，简化实现）→ 结果与基线对比。里程碑第 0/30/60/90 天；到期时「今日」顶部出现提醒，进度页有测评对比表和「现在测」入口。
- 修复：对话任务「礼貌地结束对话」被提前勾上。任务可标 `strict: true`，只有用户真的说了道别才算完成（`roleplay.js` 的 `saidBye`）。
- 修复：打中文时英文说法重复出现两次。现在只在用户消息下的点评卡显示（标题「用英语可以这样说」），AI 回复下的「可以说」只在英文问“how do I say”时出现。
- 修复：说完自己工作后问 “How about you?”，「问对方是做什么的」没打勾。任务检查现在带上整段对话上下文，并新增任务字段 `match`（正则兜底，命中即完成）。
- 新增：AI 对话里 AI 的回复旁有「+ 表达卡」，一键把整句加入表达卡（来源记为「AI 对话」）。
- 数据导出：`GET /api/export` 下载全部数据 JSON（Notion token 会被清空）；设置页「数据」分组有导出链接。
- 录音清理：设置 `audioKeepDays`（默认 30），启动时及每天自动删除过期录音文件，数据库记录保留、`audio_path` 置空；`POST /api/audio/cleanup` 可手动触发。
- 一键卸载：`bash scripts/uninstall.sh`（先用 `--dry-run` 看会删什么）。列出内容和大小 → 可先导出 JSON 到「下载」→ 输入「删除」确认 → 停服务、删数据/模型/依赖/构建 → 逐个询问 `ollama rm`、是否卸载 Ollama、whisper-cpp、整个项目文件夹。不删 Notion 数据和系统声音。
- 测试：新增 `assessment`、`maintenance`、`plan-week`（7 天模拟）测试，共 46 项通过。

### PRD 第 8 节验收（2026-09-19）

| # | 标准 | 结果 |
|---|------|------|
| 1 | 断网时除 Notion 外都能走通 | 通过（设计上全本地；自动化测试用假模型走通全部接口）。用户 Mac 上未专门断网实测 |
| 2 | 首次启动完成入门测评、生成基线 | 通过（今日页提醒 → 测评 → 基线入库，云端截图验证）。注意：不是强制首次引导，用户可跳过 |
| 3 | 7 天模拟每种模块 ≥ 2 次 | 通过（`tests/plan-week.test.js`，模拟 200 轮最少也有 2 次） |
| 4 | 对话单轮 ≤ 5 秒 | 部分：用户反馈 qwen2.5:7b 约 3–5 秒；gemma4:12b 加了 keep_alive 后用户确认「回复时间没问题」，未精确计时 |
| 5 | 跟读标漏读和长停顿；独白三项指标 | 通过（`score.test.js`、`practice.test.js`）。用户 Mac 上麦克风实测跟读已用过 |
| 6 | 表达卡按 3.3 规则排期 | 通过（`srs.test.js`、`cards.test.js`） |
| 7 | 同步后 Notion 可见 | 部分：假 Notion 服务测试通过；**用户真实账号未确认** |
| 8 | 界面与原型一致 | 基本一致（导航、布局、主要按钮），后续按用户反馈调整过细节 |
| 9 | 卸载脚本清除 3.11 内容，保留 Notion | 部分：脚本已写，`--dry-run` 可预览；**未在用户 Mac 上真实执行** |

### 仍未做

- 内容审核开关（PRD 第 7 节）、浏览器每日提醒通知。
- 设置里的「每日时长」目前不影响课程编排（固定 30 分钟）。
- 表达卡复习状态同步到 Notion（现在只同步笔记本）。
- 第 5–12 周内容（现在有 1–4 周，按 README「如何添加学习内容」补）。
- 自选卡组、卡片正面录音。

## 每周 6 个场景、按天解锁（用户新需求，2026-09-19，已完成）

- 第 1–3 周各补到 6 个场景（新增 12 个），每天配 15 张卡（新增 54 + 126 张）、1 组跟读（新增 12 组）、1 个话题（新增 3 个）。原有内容按主题分到各天。第 6 天场景为「挑战」（level = advanced）。
- 按天解锁：`cards.js` 的 `planPosition` 新增 `dayInWeek`（1–7），`UNLOCK_SQL` / `isUnlocked` 用于表达卡新卡、场景、跟读、话题。
- 每日课程（`plan.js` `generate`）：第 1–6 天 = 热身 → 今日场景 → 跟读 / 独白（轮流）→ 收尾；第 7 天复习日（原来是自然周日）。没有按天内容的周退回旧规则。旧格式的当天课程会自动重排。
- 今日页「独白」直达当天话题（`/mono?t=`）；场景列表显示「第几周第几天」。
- PRD 2.3 已同步修订。测试 47 项通过（`plan-week` 改为每轮清空课程再模拟）。
- 注意：按天解锁后内置新卡每天 15 张；用户从 AI 对话「+ 表达卡」补充，加的卡记在当天、立即可学，和内置卡一起受「每日新卡」上限约束。

## 雅思词汇导入表达卡（用户新需求，2026-09-20，已完成，真实账号待验证）

- 来源：用户的 ChatGPT 每天同步到 Notion「IELTS Listening Daily」数据库。每页正文 ①Listening ②Questions ③Transcript & Answers ④Vocabulary（表格：Word / Phrase、中文释义、Common Collocations、Original Example）⑤Review；属性有 Title、Date、Topic、Level、Status、New Words、Error Type。
- `server/ielts.js`：查最近 14 页，找「Vocabulary」标题后的第一个表格，按表头对列，每个词生成一张卡：正面中文，背面词 + 音标，「原文：」例句，下方「搭配：」。来源「雅思」，场景「雅思 · Topic」，记在当天（立即可学），算在每日新卡上限内。
- 用户选择「一同步就导入」（不等 Status）。已导入页面记在设置 `ieltsPages`，英文相同的卡跳过。
- 触发：启动 30 秒后和每 10 分钟（`autosync.js`，不受「自动同步」开关影响）；设置页「Notion 同步 → 4. 雅思词汇」可手动「立即拉取」。`POST /api/ielts/pull`。
- `card` 新增 `note`（搭配）；卡片接口对雅思卡返回 `ipa`。
- 调试脚本 `scripts/notion-peek.mjs <数据库链接>`：把数据库字段和最近 3 页内容存到 `data/notion-peek.json`。
- 以后可做（用户说先不复杂化）：听力原文进跟读（2026-09-24 已做，见「雅思跟读」）、雅思口语 Part 1/2/3 进独白和 AI 对话。

## 划词例句修复（2026-09-20）

- 问题：查 “been up to” 时例句给的是 “I've just been working a lot lately.”（这是回答，不含该短语）。
- 改法：提示词要求例句必须包含所查词（可变时态人称）；`notes.js` 新增 `mentions()` 校验，不含就退回查词时的原句，再不行就不显示例句。测试见 `tests/notes.test.js`。

## 表达卡「自己先说」检查（用户新需求，2026-09-21，已完成）

- 卡片下方加输入框 + 麦克风（识别后填进输入框）+「检查」，回车也能检查。
- `POST /api/cards/:id/check`（`cards.js` 的 `checkAnswer`）：把卡片的中文意思和参考英文给模型，判断学习者的说法 ok / close / wrong，给一句中文点评；不是 ok 时给一个更自然的说法（可朗读）。
- 只给结果，不自动评分（用户选的）；换卡时清空。假模型 kind = 'check'。

## 查词提速（2026-09-21）

- 缓存：新表 `lookup_cache`（key = 词 + 语境前 120 字），查过的组合直接返回。
- 输出变短：提示词要求更简洁，并给模型加 `num_predict: 220`（`llm.chat` 新增 `maxTokens`）。
- 前端：鼠标移到「查词」上就开始预取；音标走新的 `GET /api/ipa`（本地词典，立即显示），不等模型。
- 没有换模型，也没有减少展示内容。

## 对话点评改写更贴合场景（2026-09-22）

- 问题：学员说 “Please address me at alex123.”（想说微信号），点评给的是 “You can reach me at alex123.”，语法对但搭配不对（reach me at 接电话或邮箱）。
- 改法：`roleplay.js` 的 `feedback` 提示词加了 4 条约束——改写必须是母语者在该场景真会说的话、尊重固定搭配（举了 reach me at / WeChat ID 的例子）、保持学员原意和口语语气、是中式英语时要在 issue_zh 里点明；同时把场景的 `brief` 一起给模型。

## 独白话题不再重复（2026-09-22）

- 用户反馈连着两天都是「你的一个爱好」。原因：当天课程排的是跟读，用户自己点进独白页时是在已解锁话题里纯随机抽，抽中了昨天刚练的。数据没丢（已核对数据库）。
- `practice.js` 的 `topicList` 增加 `doneAt`（该话题最近一次练习日期，来自 session.metrics.topicId）；`Mono.jsx` 的随机挑选改为优先没练过的，都练过则挑最久没说的。
- 话题上方的「第 1 周第 4 天」改成「第 1 周 · 1 分钟」，避免和「今天第几天」混淆。

## 点评不再改掉本来就对的说法（2026-09-23）

- 问题：学员在视频会议场景说 “This is Alex I'm in charge of the product。”，点评把 This is 改成 I'm，理由不成立（电话/视频通话里 This is + 名字是标准说法）。真正的毛病只是两句连写和中文句号。
- 改法：`roleplay.js` 的 `feedback` 提示词再加两条——只改真正错的地方，学员的说法本来地道就回 ok（举了 This is <name> 的例子）；只是标点、大小写或断句问题也回 ok（口语不看标点，和既有的 loose 比较规则一致）。

## 数据库防损坏与每日备份（2026-09-24）

- 写入改为「先写 `speak90.db.tmp` 再改名替换」，写到一半断电或崩溃，原文件仍完好。
- 每日备份：`data/backups/speak90-YYYY-MM-DD.db`，启动时（迁移前）和服务运行中每小时检查一次，当天已有就跳过；保留最近 7 份。卸载脚本删除 `data/` 时一并删除。
- 启动时数据库读不出来：原文件改名为 `speak90.db.corrupt-<时间>` 保留，自动从最近一份备份恢复；没有备份才新建空库。终端会打印提示。
- 测试：新增 `tests/db.test.js`，共 55 项通过。

## 对话点评评测集（2026-09-24）

- `evals/feedback.json`：30 条例句，16 条「本来就地道，不该改」、14 条「有问题，应该指出」，可对改写加正则要求（`better_must` / `better_not`）。含开发中遇到的真实误判（This is <name>、微信号 reach me at）。
- `node scripts/eval-feedback.mjs [--model 名称] [--runs 3] [--only 关键词]`：用真实模型逐条跑，打印每条结果和汇总，详细结果存 `data/evals/`（不提交）。改点评提示词前后各跑一次对比。
- 为此把点评逻辑从 `feedback()` 拆成 `roleplay.js` 的 `judgeLine(scene, prevLine, line, model)`，行为不变。
- 打中文的句子不走点评（由 `translateLine` 直接给英文说法），不在评测集里。
- 基准（温度 0.2，各跑 1 次）：gemma4:12b 27/30（放过 14/16，指出 13/14）；qwen2.5:7b 26/30（放过 15/16，指出 11/14）。
  - gemma4:12b 仍会把 “This is utopia I'm in charge…” 改成 I'm、把微信号改写成 “reach me at utopia”，即 09-22、09-23 两次提示词修正没有完全压住；还会把 “I work in product” 改成 “product management”。
  - qwen2.5:7b 漏判较多（glad to know you、speak slowly a little、order dish 都判为自然）。
- 测试：新增 `tests/eval-feedback.test.js`（校验评测集格式、假模型跑通脚本），共 57 项通过。

## 第 4 周内容：接待客户与介绍公司（2026-09-24）

- `content/week04.json`：90 张卡（每天 15 张）、6 个场景、6 组跟读、6 个话题，格式同前 3 周。
- 用户选择：先写一周试用再批量；12 周主题改为生活、工作交替（`plan.js` 的 `THEMES` 与 PRD 2.2 已同步）：4 接待客户与介绍公司 → 5 购物 → 6 电话与约时间 → 7 出行与旅行 → 8 开会与讨论；9–12 周不变。
- 按一次客户来访编排：第 1 天前台迎接（德国客户 David）→ 第 2 天午饭和外籍同事聊工作 → 第 3 天会议室介绍公司（新加坡集成商）→ 第 4 天展厅讲解产品（迪拜经销商，AI 会故意问一个答不上的技术问题，练「我确认后回复」）→ 第 5 天展会展位 → 第 6 天挑战：回答交货期、总结、约下一步、送别。
- 产品写成「智能硬件 + 配套软件平台」，不指向具体公司；客户是经销商和集成商。
- 难度：卡片英文平均 6.7 词（第 1–3 周 4.6–5.7），加入连接词、数字年份、比较级。与前 3 周英文重复的卡已替换。
- 测试里写死的「18 个场景 / 18 组跟读」改为按 `content/` 周数计算，以后加周不用改测试。共 57 项通过。
- 待用户试用后反馈难度和贴合度，再写第 5 周（购物）。

## 周复习对话（用户新需求，2026-09-24）

- 复习日（每周第 7 天）课程改为：复习卡 10 分钟 → **本周综合对话 20 分钟** → 本周错句重说 5 分钟。
- 内容：`content/weekNN.json` 新增 `review`（人物、开场、`stages` 6 段，每段 `day / topic / focus / task`）。导入时生成场景 `wNN-review`（day 7、level = review）。第 1 周英语角（Olivia）、第 2–3 周和老朋友 Jess 喝咖啡、第 4 周 David 回柏林后视频跟进。
- 目标表达（`roleplay.js` 的 `pickTargets`）：从本周卡里挑 12 个，优先没想起（1）→ 想起但卡（2）→ 学过没评 → 脱口而出 → 未学；同样薄弱时各天轮流取；按卡片所在的天分到对应段。存在 `roleplay.state`（新列）。
- 分段引导（`advanceStage`）：每轮只告诉模型当前段的话题和该段还没用上的表达；当前段任务完成或聊满 3 轮就进入下一段。提示词明确「让学习者说，AI 自己不要用这些原话」。
- 表达是否用上（`usedExpression`）：文字匹配不靠模型；整句出现，或关键词（排除 I/the/what/today 等常见词）三分之二以上出现在同一句；两句的卡说中一句即可。
- 当天没结束的周复习对话，再点进去接着聊（普通场景仍是每次新开）。
- 页面：对话右侧「本周表达」清单（中文，点开看英文，用上自动打勾），任务列表标「正在聊」；复盘显示「本周表达用上 x/12」和没用上的表达（可朗读）；场景列表显示「周复习 · 约 20 分钟」。
- 首页文案：练习日改为「今日场景 + 跟读或独白（轮流）」（原「中间两项按阶段权重随机抽取」已不符）；「换一个」只在有随机项时显示。
- 真实模型试跑（gemma4:12b，第 1 周）：6 段按顺序推进，8 轮通关，用上 10/12；每轮 7–10 秒（回复 + 任务检查）。试跑中修正：AI 抢先说目标表达（提示词收紧、第 5 段 focus 去掉“提议保持联系”）、常见词误判、第 1 周第 6 段与视频会议卡对不上（改为聊视频会议再道别）。
- 测试：新增 `tests/review.test.js`，共 63 项通过。
- 以后写新一周内容时要带 `review`（格式见 README）。

## 笔记本缺中文释义修复（2026-09-24）

- 问题：划词弹窗显示「查询中…」时，「加入笔记本 / 加入表达卡」已经能点，点了就存进一条没有中文和词性的笔记（表达卡会是正面空白）。
- 改法：
  - `SelectionLookup.jsx`：点加入时查询还没回来，先显示「等查询结果…」，等结果回来再保存。
  - `notes.js` 新增 `fillMissingZh()`：中文为空的笔记后台重新查词补上；新增笔记没有中文时立即触发，服务启动 20 秒后也跑一次（补旧数据）。模型不可用时下次再补。
  - Notion：补上后标记待同步；同步时已有 `notion_page_id` 的笔记更新原页面（PATCH），不新建重复页。
- 测试：`notes.test.js`、`notion.test.js` 各加 1 项。

## 雅思词汇：段落格式也能导入（2026-09-24）

- 问题：对照真实数据库，ChatGPT 写 ④ Vocabulary 时有时用表格、有时用段落（「word — 中文」+「Common collocations: …」+「Original example: …」）。导入只认表格，段落格式的页面词汇一直没导入，也不报错。
- 改法：`ielts.js` 的 `vocabOf` 只读 Vocabulary 这一节（到下一个标题为止）；有表格按表格读，没有就用 `parseVocabLines` 按段落读。未导入的页面没记在 `ieltsPages`，修复后下次拉取会自动补上。
- 测试：`ielts.test.js` 加一页段落格式（示例为自拟内容，不含用户笔记）。

## 雅思跟读：听力原文反哺跟读（用户新需求，2026-09-24，已完成，真实账号待验证）

- 用户在三个方案（跟读 / 独白 Part 3 / AI 讨论）里先选了跟读。
- 拉取雅思词汇时一并读 ① Listening 原文（`server/ielts.js` 的 `readPage`）：这一节到下一个同级标题或带序号（②③…）的标题为止，中间的小标题（Part 1）、折叠块、引用里的内容也读。去掉「Speaker A:」这类说话人标签、中文行、只有括号的行。
- 拆句（`splitSentences`）：Dr. / Mr. / e.g. 等缩写和 3.5 这类数字不拆开。挑句（`pickShadowSentences`）：优先含当天生词多的句子，最多 8 句；不够 6 句用原文里 6–25 词的句子补齐；按原文顺序；少于 3 句不生成。生词匹配允许词尾变化（impose → imposed）、短语中间隔两个词（take sth into account）；不规则变化（take → took）匹配不到。
- 每句的中文一行写「生词：词 释义」（不调模型翻译）。
- 存为 `content_item`（id = `ielts:<页面ID>`，type = shadow，extra 带 `source: 'ielts'` 和日期），记在当天，立即可练。跟读页下拉框底部单独一组「雅思跟读（来自 Notion 听力原文）」，按日期倒序。每日课程不排雅思跟读（`plan.js` 排除 `ielts:` 开头的材料），在跟读页自选。
- 已生成过的页面记在设置 `ieltsShadowPages`；之前已导入词汇的页面（最近 14 页）下次拉取时会补生成跟读。`POST /api/ielts/pull` 返回值新增 `shadows`，设置页提示「新增 N 组雅思跟读」。
- 测试：`tests/ielts.test.js` 新增 2 项（假 Notion 页面、自拟原文），共 67 项通过。
- 下一步候选：独白用雅思口语 Part 3 风格问题（2026-09-24 已做，见「雅思独白」）；AI 对话讨论短文。

## 雅思独白：口语 Part 3 问题（用户新需求，2026-09-24，已完成，真实模型出题质量待用户验证）

- 拉取雅思时，按每页的话题（Topic）和 ① Listening 原文，让本地模型出 3 个口语 Part 3 问题（`ielts.js` 的 `part3Questions`，kind = 'part3'）：抽象讨论题，不问原文细节，不是 Part 1 个人题；三种类型各一个（观点+理由 / 对比 / 未来或解决办法）；每题 ≤ 25 词，带中文翻译。不合格的（太短太长、英文里有中文）丢掉。
- 存为独白话题 `content_item`（id = `ielts:<页面ID>:q1…q3`，type = topic，extra 带 `source: 'ielts'`、`date`、`group`（话题名）、`hints`（当天前 4 个生词））。
- 本地模型较慢：每次拉取最多给 3 页出题，其余留到之后的自动拉取（每 10 分钟）。模型没开或没给出合格问题时不记为已出题，下次再试。已出题的页面记在设置 `ieltsPart3Pages`。
- 独白页：右上角新增下拉框「日常话题（随机）/ 雅思口语 Part 3」。选雅思问题后显示英文问题（可朗读）、中文、「试着用上：生词」「先亮观点，再给理由和例子」；「换个话题」只在雅思问题里换。默认和随机仍只抽日常话题，每日课程不排雅思问题。
- 点评：雅思问题的改写目标改为「约 band 7 的完整回答（观点 + 理由 + 例子）」；日常话题的提示词不变。从雅思问题加的表达卡场景记为「雅思 · 话题」。
- `POST /api/ielts/pull` 返回值新增 `questions`。测试新增 1 项（假模型），共 68 项通过。云端没有本地模型，出题质量未用 gemma4:12b 实测。

## 对话点评代码兜底（2026-09-24，已完成）

- 问题：09-22、09-23 两次改提示词后，gemma4:12b 仍会把 “This is <名字>” 改成 I'm，把微信号改写成 “reach me at <用户名>”（评测集 ok-this-is-name、ok-this-is-name-run-on、fix-wechat-reach-me）。
- 改法：提示词不动，在 `roleplay.js` 的 `judgeLine` 之后加代码兜底 `guardFeedback(line, fb)`：
  1. 学员说了 “This is <名字>”，模型改成 I'm / I am / My name is + 同一名字 → 改回 This is；改完后和原话只差大小写、标点、断句或 and / so / yes 这类连接词 → 判为没问题。别处真有错时保留模型的修改。
  2. 模型改写里有 “reach / contact me at <X>”，X 不是邮箱（没有 @）也不是电话（少于 5 位数字）→ 换成 “(You can) add me on WeChat. My WeChat ID is X”，说明和中文一起换掉。
- 云端连不上 Ollama，没法跑 gemma 评测；测试新增 `tests/feedback-guard.test.js` 3 项，共 71 项通过。
- 用户本地评测（gemma4:12b，各跑 1 次）：29/30（放过 15/16，指出 14/14），基准 27/30。ok-this-is-name、ok-this-is-name-run-on、fix-wechat-reach-me 均通过。新的基准为 29/30。
- 仍未处理：gemma 把 “I work in product” 改成 “product management”。

## 今日页复习日文字遮挡（2026-09-24）

- 第 7 天「本周综合对话 20 分钟」超出时间列固定宽度 110px，压住右侧状态圆点。`Today.jsx` 时间列改为最小 150px、不压缩，状态列不压缩。

## 表达卡：识别失败不再整张卡消失（2026-09-24）

- 问题：`Cards.jsx` 只有一个 `err`，一有值就整页只显示提示；麦克风「没有听清」也走这里，卡片没了。
- 改法：拆成 `fatal`（连不上服务，整页提示）和 `err`（录音、检查、评分出错，输入框下方一行红字，卡片保留）；换卡或重新输入时清掉提示。浏览器模拟麦克风 + 识别为空验证过。

## 表达卡：换卡后不再显示上一张的检查结果（2026-09-24）

- 问题：点「检查」后在结果回来前就评分换卡，结果回来时写到了新卡下面（麦克风识别结果同理）。
- 改法：`Cards.jsx` 加 `turnRef`，每次评分换卡 +1；检查、识别的结果回来时轮次变了就丢弃。评分时如果正在录音，一并取消录音。浏览器里把检查接口延迟 2 秒复现过，修复前出现、修复后不再出现。

## 第 7 天复习日不出新卡（2026-09-24）

- 问题：复习日表达卡里出现了没学过的雅思新卡。`todayQueue` 每天都补新卡，第 7 天也不例外（PRD 2.3 写的是到期卡）。
- 改法：第 7 天只出到期卡，不出来源为「内置」「雅思」的新卡，顺延到下周第 1 天；当天自己加的卡（AI 对话、独白、笔记本、手动）照常可学。测试新增 1 项，共 72 项通过。

## AI 对话发消息后整页上弹（2026-09-24）

- 问题：每发一句，页面把最后一条消息顶到窗口最上方，对话区跑出屏幕。原因：`scrollIntoView` 默认对齐顶部，而右侧面板（周复习时有任务 + 12 个本周表达）比对话长，整页被撑高。
- 改法：右侧面板最高一屏、内部滚动（`styles.css` `.panel`）；新消息用 `block: 'end'` 只滚到停在输入框上方（`scrollMarginBottom`），点评框晚到时再滚一次。浏览器模拟 4 轮对话验证。
- 顺带：本周表达里有一条没有中文（对话里加的卡没带中文），`pickTargets` 不再选没有中文的卡；已经选上的直接显示英文。

## 协作流程（用户确认，2026-09-24）

- 开发在云端（Claude Code Cloud Session）进行，GitHub 做中转。每个需求一个分支、一个 PR。
- **`npm test` 和 `npm run build` 都通过就直接合并到 master**，不用再等用户说「合并」。改动大或有风险（数据库结构、点评提示词、删数据）时先不合并，说明怎么试用。
- 用户本地更新只需一条命令：`bash scripts/update.sh`（检查未提交修改 → 切到 master → `git pull --ff-only` → 执行 start.sh）。试用分支：`bash scripts/update.sh <分支名>`。
- 不要让 PR 互相叠着等待合并。

## 已知问题 / 注意

- 已在用户 Mac 上验证：对话模型 qwen2.5:7b、whisper small.en、系统朗读声音均已就绪（2026-09-18）。
- 启动后终端窗口必须保持打开，关掉即服务停止。
- 国内网络：brew 需用清华镜像并关闭自动更新；whisper 模型可从 hf-mirror.com 下载。
- 默认对话模型 `qwen2.5:7b`，可在设置页改。
- 数据库使用 sql.js（整库在内存，改动后 300ms 写回 `data/speak90.db`），单用户足够。每天自动备份到 `data/backups/`，保留 7 份。

## 数据库变更记录

- 阶段 1：初始表结构（见 `server/schema.sql`）。
- 阶段 2：`card` 增加 `content_id, introduced_at, reviewed_at, last_rating`。
- 阶段 3：`content_scene` 增加 `data`（场景完整 JSON）；`roleplay` 增加 `ended_at`、`feedback`。
- 划词：`note` 增加 `context`、`detail`。
- 阶段 5a：新表 `daily_log`；`plan_day` 增加 `cards_target`、`wrap_done`。
- 阶段 5b：新表 `notion_sync`；设置增加 `notionParentPage`、`notionLastSync`。
- 阶段 6：无新表（测评用原有 `assessment` 表）；设置增加 `audioKeepDays`；场景任务支持 `strict` 字段。
- 按天解锁：`card`、`content_item`、`content_scene` 增加 `day`（默认 1）；场景任务支持 `match` 字段。
- 周复习对话：`roleplay` 增加 `state`（目标表达、当前段）；`content_scene.level` 新值 `review`。
- 雅思导入：`card` 增加 `note`；设置增加 `ieltsDb`、`ieltsLastPull`、`ieltsPages`。
- 查词缓存：新表 `lookup_cache`。
- 雅思跟读：无新表（`content_item` 里 id 以 `ielts:` 开头）；设置增加 `ieltsShadowPages`。
- 雅思独白：无新表（`content_item` type = topic，id 以 `ielts:` 开头）；设置增加 `ieltsPart3Pages`。
